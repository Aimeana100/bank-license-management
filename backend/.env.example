# Environment variables for the backend application

# app
PORT=5000

# database
DB_HOST=#database host (postgres when using docker)
DB_PORT=5432
DB_USERNAME= #database username (postgres when using docker)
DB_PASSWORD=#database password
DB_NAME=#database name
